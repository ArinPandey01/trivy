{
  "name": "frontend",
  "version": "1.0.0",
  "description": "This is built to drop into the `frontend/` folder you already initialized with `npx wxt@latest init`. It doesn't replace your existing WXT config files wholesale — merge the pieces below into what you already have.",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "wxt",
    "build": "wxt build",
    "zip": "wxt zip"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@wxt-dev/module-react": "^1.2.2",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "wxt": "^0.21.4"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.9",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.7",
    "typescript": "^7.0.2"
  }
}
